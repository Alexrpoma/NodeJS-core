/*
  GLOBAL VARIABLE:

  nodeJs                      navigator
  

  global      globalThis      window

  from global variable we can access to every functions of javascript.
*/

//Example:

globalThis.console.log(global);
globalThis.console.log("Hello");
globalThis.console.log(globalThis.Math.pow(2,2));
